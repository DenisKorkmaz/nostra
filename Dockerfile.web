FROM node:24-alpine AS build

WORKDIR /src

COPY web/package.json web/package-lock.json ./
RUN npm ci

COPY web/ ./
RUN npm run build

FROM caddy:2-alpine

ARG VERSION=dev

COPY deploy/Caddyfile /etc/caddy/Caddyfile
COPY --from=build /src/dist /srv

RUN printf '%s' "${VERSION}" > /srv/version.txt
